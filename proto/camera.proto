syntax = "proto3";
package wilson;

import "vector.proto";

//Virtual camera dictating the point of view rendered in 3D mode
message Camera {
    //position of camera
    Vector position = 1;
    //target the camera looks at
    Vector target = 2;
}
