syntax = "proto3";
package wilson;

//RGBA color denoted in unit floats
message Color {
    //Red channel [0,1]
    float r = 1;
    //Green channel [0,1]
    float g = 2;
    //Blue channel [0,1]
    float b = 3;
    //Alpha channel [0,1] where 0 is transparent and 1 opaque
    float a = 4;
}
