syntax = "proto3";
package p1on;

//Methods to interpolate the discrete values in time
enum Interpolation {
    //Hold value until a newer one appears
    STEP = 0;
    //Linear interpolation inbetween stops
    LINEAR = 1;
    //Cubic (smooth) interpolation inbetween stops
    CUBIC = 2;
}
