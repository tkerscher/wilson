syntax = "proto3";
package p1on;

import "properties.proto";

//Animatible sphere in 3D space
message Sphere {
    //Name as shown in explorer
    string name = 1;
    //Additional text shown when selected
    string description = 2;
    //True, if visible in 3D viewer
    bool isVisible = 3;
    //Color
    ColorProperty color = 4;

    //center of sphere
    VectorProperty position = 5;
    //Radius
    ScalarProperty radius = 6;
}
