syntax = "proto3";
package wilson;

import "properties.proto";

//Animatible sphere in 3D space
message Sphere {
    //center of sphere
    VectorProperty position = 1;
    //Radius
    ScalarProperty radius = 2;
    //Color
    ColorProperty color = 3;
}
